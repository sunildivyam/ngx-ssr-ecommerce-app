rules_version = '1';
service cloud.firestore {

  match /databases/{database}/documents {
    match /addresses/{document=**} {
      allow create, update, delete, read: if
      request.auth.uid == resource.data.uid
    }
  }
}
